const routes = {
  login: "/",
  chat_manager: "/chat/:configId",
  ctv_chat: "/chat-manager/:configId/:ctvId",
  add_config: "/add-config",
  profile: "/profile",
  manager_page: "/manager-page",
};

export default routes;
